/**
 * Document types shown in the searching step carousel.
 * Only include types that exist in the file sidebar / codebase. Currently only PDFs.
 * Add 'docx' when Word documents are supported and present in the app.
 */
export const SEARCHING_CAROUSEL_TYPES: ('pdf' | 'docx')[] = ['pdf'];
