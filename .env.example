# Environment Variables Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Flask Configuration
# SECRET_KEY: Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://user:password@localhost/database_name
REDIS_URL=redis://localhost:6379/0

# Doc extraction: required for chat file attachments (PDF, Word, Excel, PowerPoint, etc.).
# The Node doc-extraction service runs on port 5002 (started by ./start-velora-go.sh when present).
# Set this to enable extraction; start script exports it when the service starts successfully.
EXTRACTION_SERVICE_URL=http://localhost:5002

# AWS Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
S3_UPLOAD_BUCKET=your-s3-bucket-name
API_GATEWAY_INVOKE_URL=https://yourapi.execute-api.region.amazonaws.com/stage

# AI Services
LLAMA_CLOUD_API_KEY=llx-your-llama-key
OPENAI_API_KEY=sk-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key  # For Claude extended thinking

# Google Gemini Embeddings (optional - when enabled, replaces Voyage for RAG embeddings)
# USE_GEMINI_EMBEDDINGS=true
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_EMBEDDING_MODEL=models/gemini-embedding-2-preview
# GEMINI_EMBEDDING_DIMENSION=768

# Research Agent (Model-driven tool choice)
# When enabled, document searches use an agent that decides which tools to call
RESEARCH_AGENT_ENABLED=false
RESEARCH_AGENT_MAX_ITERATIONS=10
RESEARCH_AGENT_TIMEOUT_SECONDS=120

# Supabase Configuration (replaces AstraDB)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key

# Google Maps (for geocoding)
GOOGLE_MAPS_API_KEY=your-google-maps-key

# PostgreSQL Password (for local Docker setup)
POSTGRES_PASSWORD=your-postgres-password

# Mem0 persistent memory (user preferences, context across chats)
# Set to false to disable if you hit rate limits or want to avoid extra OpenAI/embedding calls
MEM0_ENABLED=true
MEM0_SEARCH_LIMIT=5
MEM0_SEARCH_TIMEOUT=2.0

# LangSmith / LangChain tracing (LLM and graph execution)
# Set LANGCHAIN_TRACING_V2=true and LANGCHAIN_API_KEY to send traces to LangSmith for debugging.

LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=

# Optional: LANGSMITH_* are mapped to LANGCHAIN_* if the latter are unset
# LANGSMITH_TRACING=
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=
